alt + Return
      samedir

alt + Escape
      pkill -USR1 -x sxhkd

alt + shift + w
      $BROWSER

Print
      maim -s screenshot-$(date '+%d%m%y-%H:%M').png

alt + Print
      maim screenshot-$(date '+%d%m%y-%H:%M').png

alt + Pause
      record

alt + F1
      shortcuts

alt + F2
      $TERMINAL -e gotop

alt + F3
      displayselect

alt + F4
      screenshot

alt + F5
      todo

alt + F6
      icons

alt + F10
      showclip
