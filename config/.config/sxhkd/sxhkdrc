alt + Return
      samedir

alt + Escape
      pkill -USR1 -x sxhkd

alt + shift + w
      $BROWSER

Print
      maim -s screenshot-$(date '+%d%m%y-%H:%M').png

alt + Print
      maim screenshot-$(date '+%d%m%y-%H:%M').png

alt + Pause
      record

alt + F1
      shortcuts

alt + F2
      bluetooth

alt + F3
      favorite-vids

alt + F4
      todo

alt + F5
      screenshot

alt + F6
      icons

alt + F7
      fullscreen

alt + F8
      redshift -O 4000

alt + F9
      redshift -x

alt + F10
      showclip

alt + F11
      displayselect
